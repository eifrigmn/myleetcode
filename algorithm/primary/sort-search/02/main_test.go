package _02

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestFirstBadVersion(t *testing.T) {
	Convey("第一个错误的版本", t, func() {
		// n = 5, bad = 4
		// 4
		var n = 5
		var bad = 4
		// So(firstBadVersion())
	})
}
